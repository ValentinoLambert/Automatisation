services:
  node:
    image: node:20-alpine
    container_name: immo-admin-react-node
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev"
